import React from "react";

export default function setHeight(ref: React.MutableRefObject<null>) {
  console.log(ref.current)

  // dispatch({type: 'jsx_elements/header', element: <></>, params: {ref: headerRef}})

}
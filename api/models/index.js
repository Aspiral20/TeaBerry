const TokenSchema = require('./token-model.schema')
const UserSchema = require('./user-model.schema')
const ProductSchema = require('./product-model.schema')

module.exports = {
  TokenSchema,
  UserSchema,
  ProductSchema,
}